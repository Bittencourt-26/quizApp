import 'package:flutter/material.dart';

class TelaContribuicao extends StatelessWidget {
  const TelaContribuicao({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}